import type { Metadata } from "next"; import Link from "next/link"; import { SITE_CONFIG, absoluteUrl } from "@/config/site"; import { RelatedSeoLinks } from "@/features/seo/RelatedSeoLinks"; import { ALL_SEO_GUIDES } from "@/features/seo/guides";
export const metadata:Metadata={title:`Interior Design Guides for Pune Homes — ${SITE_CONFIG.name}`,description:"Practical ONEDECORE guides for Pune homeowners covering interior planning, modular kitchens, wardrobes, quotations, materials and renovation.",alternates:{canonical:absoluteUrl("guides")}};
export default function Page(){return <main className="od-portfolio-main"><header className="od-portfolio-header"><p className="od-portfolio-eyebrow">ONEDECORE Guides</p><h1 className="od-portfolio-title">Interior planning guides for Pune homeowners.</h1><p className="od-portfolio-lede">Useful reading for decisions that happen before finishes are selected and manufacturing begins.</p></header><section className="od-prose od-detail-section">{ALL_SEO_GUIDES.map(g=><article key={g.slug}><h2><Link href={`/guides/${g.slug}`}>{g.title}</Link></h2><p>{g.description}</p></article>)}</section><RelatedSeoLinks exclude={["/guides"]} /></main>}
