import type { ReactNode } from "react";

/** Segment layout — chrome is provided by LegalPageShell. */
export default function LegalSegmentLayout({
  children,
}: Readonly<{ children: ReactNode }>) {
  return children;
}
