import type { Metadata } from "next"; import Link from "next/link"; import { SITE_CONFIG, absoluteUrl } from "@/config/site"; import { PUNE_AREA_PAGES } from "@/features/seo/pune-areas";
export const metadata: Metadata={title:`Interior Design Services Across Pune — ${SITE_CONFIG.name}`,description:"Explore ONEDECORE interior design service-area information for Pune, with planning guidance for homes across key residential areas.",alternates:{canonical:absoluteUrl("pune")}};
export default function Page(){return <main className="od-portfolio-main"><header className="od-portfolio-header"><p className="od-portfolio-eyebrow">Pune Service Areas</p><h1 className="od-portfolio-title">Interior design services across Pune.</h1><p className="od-portfolio-lede">Browse area-specific planning information, then explore the relevant ONEDECORE service or portfolio.</p></header><section className="od-prose od-detail-section"><h2>Areas we cover</h2><p>{PUNE_AREA_PAGES.map((a,i)=><span key={a.slug}>{i?", ":""}<Link href={`/pune/${a.slug}`}>{a.name}</Link></span>)}</p><h2>Start with the service you need</h2><p><Link href="/services/complete-home-interiors">Complete home interiors</Link>, <Link href="/services/modular-kitchens">modular kitchens</Link>, <Link href="/services/wardrobes">custom wardrobes</Link> and <Link href="/services/home-renovation">home renovation</Link>.</p></section></main>}
