import type { Metadata } from "next"; import Link from "next/link"; import { notFound } from "next/navigation"; import { SITE_CONFIG, absoluteUrl } from "@/config/site"; import { PUNE_AREA_PAGES, getPuneArea } from "@/features/seo/pune-areas"; import { AREA_DEEP_CONTENT } from "@/features/seo/area-content";
export function generateStaticParams(){return PUNE_AREA_PAGES.map(({slug})=>({area:slug}));}
export async function generateMetadata({params}:{params:Promise<{area:string}>}):Promise<Metadata>{const {area:slug}=await params;const area=getPuneArea(slug);if(!area)return {robots:{index:false,follow:false}};const title=`Interior Designers in ${area.name}, Pune — ${SITE_CONFIG.name}`;const description=`Planning home interiors in ${area.name}, Pune? Explore ONEDECORE's approach to complete home interiors, modular kitchens, wardrobes and renovation.`;return {title,description,alternates:{canonical:absoluteUrl(`pune/${area.slug}`)},openGraph:{title,description,url:absoluteUrl(`pune/${area.slug}`),siteName:SITE_CONFIG.name,locale:SITE_CONFIG.locale,type:"website"}};}
export default async function Page({params}:{params:Promise<{area:string}>}){const {area:slug}=await params;const area=getPuneArea(slug);if(!area)notFound();const url=absoluteUrl(`pune/${area.slug}`);const deep=AREA_DEEP_CONTENT[area.slug];const jsonLd={"@context":"https://schema.org","@graph":[{"@type":"Service","@id":`${url}#service`,name:`Interior design services in ${area.name}, Pune`,serviceType:"Interior design",areaServed:{"@type":"Place",name:`${area.name}, Pune`},provider:{"@id":`${SITE_CONFIG.url}/#organization`} },{"@type":"BreadcrumbList",itemListElement:[{"@type":"ListItem",position:1,name:"Home",item:SITE_CONFIG.url},{"@type":"ListItem",position:2,name:"Pune",item:absoluteUrl("pune")},{"@type":"ListItem",position:3,name:area.name,item:url}]}]};return <main className="od-portfolio-main"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(jsonLd)}}/><nav aria-label="Breadcrumb" className="od-portfolio-eyebrow"><Link href="/">Home</Link> / <Link href="/pune">Pune</Link> / <span>{area.name}</span></nav><header className="od-portfolio-header"><p className="od-portfolio-eyebrow">{area.zone}</p><h1 className="od-portfolio-title">Interior designers in {area.name}, Pune.</h1><p className="od-portfolio-lede">Plan complete home interiors, modular kitchens, custom wardrobes and renovation with ONEDECORE.</p></header><section className="od-prose od-detail-section"><h2>Planning interiors for a home in {area.name}</h2><p>{area.context}</p><p>{area.planning}</p><h2>What to check in a {area.name} home</h2><p>{deep?.property}</p><h2>Where to focus the interior budget</h2><p>{deep?.focus}</p><h2>If you are renovating an existing home</h2><p>{deep?.renovation}</p><h2>Interior services for {area.name}</h2><p><Link href="/services/complete-home-interiors">Complete home interiors</Link> coordinate multiple rooms as one scope. <Link href="/services/modular-kitchens">Modular kitchen</Link> planning focuses on workflow and storage, while <Link href="/services/wardrobes">custom wardrobes</Link> are planned around room dimensions and storage needs. For existing homes, see <Link href="/services/home-renovation">home renovation</Link>.</p><h2>Projects and budget planning</h2><p>See <Link href="/portfolio">ONEDECORE Pune portfolio</Link> for delivered work. For budgeting context, use the <Link href="/interior-cost">Pune interior cost guides</Link>. Project-specific pricing depends on the measured scope and specification.</p><h2>Nearby areas</h2><p>{area.nearby.join(", ")} are nearby Pune service areas. Browse the <Link href="/pune">full Pune area directory</Link>.</p><p><Link href="/#consultation">Start a free design consultation</Link> for your {area.name} home.</p></section></main>}
